#include <stdio.h>

int main(){
    printf("Hello, World!\n");
    return 0;
}


// Explanation: C uses #include to import libraries; here, <stdio.h> provides printf(), which is used for formatted output. The \n at the end adds a newline. The main function returns 0 to indicate successful execution.